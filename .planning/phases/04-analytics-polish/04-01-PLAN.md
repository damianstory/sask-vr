---
phase: 04-analytics-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/analytics.ts
  - app/layout.tsx
  - app/page.tsx
  - app/pre-vr/page.tsx
  - app/pre-vr/components/ScreenTwo.tsx
  - app/pre-vr/components/ScreenThree.tsx
  - app/pre-vr/components/ScreenFour.tsx
  - app/pre-vr/components/ScreenFive.tsx
  - app/post-vr/page.tsx
  - tests/analytics.test.ts
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
  - ANLY-04
  - ANLY-05
  - ANLY-06
  - ANLY-07
  - ANLY-08
  - ANLY-09

must_haves:
  truths:
    - "GA4 page_view events fire automatically on route navigation (landing -> /pre-vr, landing -> /post-vr)"
    - "Custom screen_view events fire on each Pre-VR screen transition (screens 1-6)"
    - "All user interactions tracked: tile select/deselect, employer pin tap, pathway expand, icon select, name entered, card download, checklist check, path select"
    - "No PII is ever sent in any analytics event (student name never included)"
    - "Analytics gracefully no-ops when NEXT_PUBLIC_GA_MEASUREMENT_ID is missing"
    - "Dev mode logs all analytics events to console instead of sending to GA4"
  artifacts:
    - path: "lib/analytics.ts"
      provides: "Centralized typed analytics helpers"
      exports: ["trackScreenView", "trackPathSelect", "trackTileSelect", "trackEmployerTap", "trackPathwayExpand", "trackIconSelect", "trackNameEntered", "trackCardDownload", "trackChecklistCheck"]
    - path: "tests/analytics.test.ts"
      provides: "Unit tests for analytics module"
      contains: "trackScreenView"
  key_links:
    - from: "lib/analytics.ts"
      to: "@next/third-parties/google"
      via: "sendGAEvent import"
      pattern: "import.*sendGAEvent.*@next/third-parties"
    - from: "app/layout.tsx"
      to: "@next/third-parties/google"
      via: "GoogleAnalytics component"
      pattern: "GoogleAnalytics.*gaId"
    - from: "app/pre-vr/page.tsx"
      to: "lib/analytics.ts"
      via: "trackScreenView call in useEffect"
      pattern: "trackScreenView"
    - from: "app/page.tsx"
      to: "lib/analytics.ts"
      via: "trackPathSelect calls in click handlers"
      pattern: "trackPathSelect"
---

<objective>
Instrument the entire micro-site with GA4 analytics using a centralized typed analytics module.

Purpose: Enable funnel analysis and interaction tracking for the Saint Luke pilot without sending any PII.
Output: lib/analytics.ts module, GoogleAnalytics in root layout, analytics calls in all interactive components, unit tests.
</objective>

<execution_context>
@/Users/damianmatheson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/damianmatheson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics-polish/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/layout.tsx (current state - will be modified):
```typescript
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={openSans.variable}>
      <body>{children}</body>
    </html>
  )
}
```

From app/pre-vr/page.tsx (screen transition state):
```typescript
type ScreenNumber = 1 | 2 | 3 | 4 | 5 | 6
const [currentScreen, setCurrentScreen] = useState<ScreenNumber>(1)
// key={currentScreen} causes remount on screen change
```

From app/page.tsx (landing page click handlers):
```typescript
<button onClick={() => router.push('/pre-vr')} ...>
<button onClick={() => router.push('/post-vr')} ...>
```

From app/post-vr/page.tsx (checklist toggle):
```typescript
const toggleItem = (id: string) => { ... }
// data.checklist is array of { id: string, label: string }
```

From app/pre-vr/components/ScreenThree.tsx (employer pin click):
```typescript
el.addEventListener('click', () => { setSelectedEmployer(employer.id) })
// employer has: id, name, description, employeeCount, quote
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create centralized analytics module with unit tests</name>
  <files>lib/analytics.ts, tests/analytics.test.ts, package.json</files>
  <read_first>
    - app/layout.tsx
    - lib/utils.ts
    - vitest.config.ts
    - tests/vitest.setup.ts
    - package.json
  </read_first>
  <behavior>
    - trackScreenView('screen_1') calls sendGAEvent with event name 'screen_view' and params { screen_name: 'screen_1' }
    - trackPathSelect('pre_vr') calls sendGAEvent with event name 'path_select' and params { path: 'pre_vr' }
    - trackTileSelect('framing', 'select') calls sendGAEvent with 'tile_select' and { tile_id: 'framing', action: 'select' }
    - trackEmployerTap('pcl', 'PCL Construction') calls sendGAEvent with 'employer_tap' and { employer_id: 'pcl', employer_name: 'PCL Construction' }
    - trackPathwayExpand('step-1', 'High School') calls sendGAEvent with 'pathway_expand' and { step_id: 'step-1', step_label: 'High School' }
    - trackIconSelect('hammer') calls sendGAEvent with 'icon_select' and { icon_id: 'hammer' }
    - trackNameEntered() calls sendGAEvent with 'name_entered' and NO parameters (zero PII)
    - trackCardDownload() calls sendGAEvent with 'card_download' and NO parameters
    - trackChecklistCheck('item-1', 'Update myBlueprint') calls sendGAEvent with 'checklist_check' and { item_id: 'item-1', item_label: 'Update myBlueprint' }
    - In development mode (NODE_ENV=development), all track functions console.log instead of calling sendGAEvent
    - When sendGAEvent is not available (no GA loaded), functions do not throw
  </behavior>
  <action>
    1. Install @next/third-parties: `npm install @next/third-parties@latest`

    2. Create lib/analytics.ts with this exact structure:
       - Import sendGAEvent from '@next/third-parties/google'
       - Define IS_DEV as `process.env.NODE_ENV === 'development'`
       - Define private `track(eventName: string, params?: Record<string, string>)` function:
         - If IS_DEV: `console.log('[Analytics]', eventName, params)` and return
         - Otherwise: `sendGAEvent('event', eventName, params)`
       - Export these typed helper functions (all calling track internally):
         - `trackScreenView(screenName: string)` -> track('screen_view', { screen_name: screenName })
         - `trackPathSelect(path: 'pre_vr' | 'post_vr')` -> track('path_select', { path })
         - `trackTileSelect(tileId: string, action: 'select' | 'deselect')` -> track('tile_select', { tile_id: tileId, action })
         - `trackEmployerTap(employerId: string, employerName: string)` -> track('employer_tap', { employer_id: employerId, employer_name: employerName })
         - `trackPathwayExpand(stepId: string, stepLabel: string)` -> track('pathway_expand', { step_id: stepId, step_label: stepLabel })
         - `trackIconSelect(iconId: string)` -> track('icon_select', { icon_id: iconId })
         - `trackNameEntered()` -> track('name_entered') // No params = no PII
         - `trackCardDownload()` -> track('card_download')
         - `trackChecklistCheck(itemId: string, itemLabel: string)` -> track('checklist_check', { item_id: itemId, item_label: itemLabel })

    3. Add GoogleAnalytics component to app/layout.tsx:
       - Import `{ GoogleAnalytics }` from `'@next/third-parties/google'`
       - Add after `<body>{children}</body>`:
         ```
         {process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID && (
           <GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID} />
         )}
         ```

    4. Create tests/analytics.test.ts:
       - Mock '@next/third-parties/google' with vi.mock: `sendGAEvent: vi.fn()`
       - For each helper: test that calling it produces the correct sendGAEvent call with exact event name and params
       - Test trackNameEntered sends NO params (PII check)
       - Test trackCardDownload sends NO params
       - Test dev mode logging: mock process.env.NODE_ENV = 'development', verify console.log is called instead of sendGAEvent
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - lib/analytics.ts contains `import { sendGAEvent } from '@next/third-parties/google'`
    - lib/analytics.ts contains `export function trackScreenView`
    - lib/analytics.ts contains `export function trackPathSelect`
    - lib/analytics.ts contains `export function trackTileSelect`
    - lib/analytics.ts contains `export function trackEmployerTap`
    - lib/analytics.ts contains `export function trackPathwayExpand`
    - lib/analytics.ts contains `export function trackIconSelect`
    - lib/analytics.ts contains `export function trackNameEntered`
    - lib/analytics.ts contains `export function trackCardDownload`
    - lib/analytics.ts contains `export function trackChecklistCheck`
    - lib/analytics.ts does NOT contain any parameter accepting a name value in trackNameEntered (zero PII)
    - app/layout.tsx contains `GoogleAnalytics` and `NEXT_PUBLIC_GA_MEASUREMENT_ID`
    - tests/analytics.test.ts exits 0
    - package.json contains `@next/third-parties`
  </acceptance_criteria>
  <done>Analytics module exports all 9 typed helpers, GoogleAnalytics component is in root layout with env var guard, all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate analytics calls into all interactive components</name>
  <files>app/page.tsx, app/pre-vr/page.tsx, app/pre-vr/components/ScreenTwo.tsx, app/pre-vr/components/ScreenThree.tsx, app/pre-vr/components/ScreenFour.tsx, app/pre-vr/components/ScreenFive.tsx, app/post-vr/page.tsx</files>
  <read_first>
    - lib/analytics.ts
    - app/page.tsx
    - app/pre-vr/page.tsx
    - app/pre-vr/components/ScreenTwo.tsx
    - app/pre-vr/components/ScreenThree.tsx
    - app/pre-vr/components/ScreenFour.tsx
    - app/pre-vr/components/ScreenFive.tsx
    - app/post-vr/page.tsx
  </read_first>
  <action>
    Add analytics calls to existing event handlers in each component. Import from '@/lib/analytics'. Do NOT restructure any component -- only add import lines and single-line track calls inside existing handlers.

    1. app/page.tsx (Landing page):
       - Import `{ trackPathSelect }` from `'@/lib/analytics'`
       - In Pre-VR button onClick, before router.push: `trackPathSelect('pre_vr')`
       - In Post-VR button onClick, before router.push: `trackPathSelect('post_vr')`

    2. app/pre-vr/page.tsx (Screen transitions):
       - Import `{ trackScreenView }` from `'@/lib/analytics'`
       - Add useEffect that fires on currentScreen change:
         ```typescript
         useEffect(() => {
           trackScreenView(`screen_${currentScreen}`)
         }, [currentScreen])
         ```
       - This fires screen_view for screen_1 on mount and on every subsequent transition

    3. app/pre-vr/components/ScreenTwo.tsx (Tile selection):
       - Import `{ trackTileSelect }` from `'@/lib/analytics'`
       - In the tile toggle handler, after updating session state:
         - If tile was just selected: `trackTileSelect(tile.id, 'select')`
         - If tile was just deselected: `trackTileSelect(tile.id, 'deselect')`

    4. app/pre-vr/components/ScreenThree.tsx (Employer pins):
       - Import `{ trackEmployerTap }` from `'@/lib/analytics'`
       - In the pin click handler (`el.addEventListener('click', ...)`), add: `trackEmployerTap(employer.id, employer.name)`

    5. app/pre-vr/components/ScreenFour.tsx (Pathway accordion):
       - Import `{ trackPathwayExpand }` from `'@/lib/analytics'`
       - In the step click/expand handler, when expanding (not collapsing): `trackPathwayExpand(step.id, step.label)`
       - Only track expand, not collapse

    6. app/pre-vr/components/ScreenFive.tsx (Card builder):
       - Import `{ trackIconSelect, trackNameEntered, trackCardDownload }` from `'@/lib/analytics'`
       - In icon selection handler: `trackIconSelect(icon.id)`
       - In name input handler: use a ref or debounce to fire `trackNameEntered()` once when name goes from empty to non-empty (not on every keystroke)
       - In download handler: `trackCardDownload()`

    7. app/post-vr/page.tsx (Checklist):
       - Import `{ trackChecklistCheck }` from `'@/lib/analytics'`
       - In toggleItem handler, when checking (not unchecking): `trackChecklistCheck(item.id, item.label)`
       - Only track check, not uncheck
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains `import.*trackPathSelect.*from.*@/lib/analytics`
    - app/page.tsx contains `trackPathSelect('pre_vr')`
    - app/page.tsx contains `trackPathSelect('post_vr')`
    - app/pre-vr/page.tsx contains `import.*trackScreenView.*from.*@/lib/analytics`
    - app/pre-vr/page.tsx contains `trackScreenView`
    - app/pre-vr/components/ScreenTwo.tsx contains `trackTileSelect`
    - app/pre-vr/components/ScreenThree.tsx contains `trackEmployerTap`
    - app/pre-vr/components/ScreenFour.tsx contains `trackPathwayExpand`
    - app/pre-vr/components/ScreenFive.tsx contains `trackIconSelect`
    - app/pre-vr/components/ScreenFive.tsx contains `trackNameEntered`
    - app/pre-vr/components/ScreenFive.tsx contains `trackCardDownload`
    - app/post-vr/page.tsx contains `trackChecklistCheck`
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0 (existing tests still pass)
  </acceptance_criteria>
  <done>All 9 analytics event types fire from their respective components. Existing tests still pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/analytics.test.ts --reporter=verbose` -- all analytics unit tests pass
2. `npx vitest run --reporter=verbose` -- full test suite passes (no regressions)
3. `npx tsc --noEmit` -- TypeScript compiles with no errors
4. `grep -r "trackScreenView\|trackPathSelect\|trackTileSelect\|trackEmployerTap\|trackPathwayExpand\|trackIconSelect\|trackNameEntered\|trackCardDownload\|trackChecklistCheck" app/ lib/` -- confirms all 9 event types are wired
5. `grep -r "window.gtag" app/` -- returns empty (no direct gtag calls, all through module)
</verification>

<success_criteria>
- GA4 GoogleAnalytics component loads conditionally in root layout
- Centralized lib/analytics.ts exports 9 typed track helpers
- Every interactive component calls the appropriate track helper
- Zero PII in any event (trackNameEntered has no parameters)
- Dev mode console.logs instead of sending to GA4
- All existing tests pass, analytics unit tests pass
- ANLY-01 through ANLY-09 addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-polish/04-01-SUMMARY.md`
</output>
