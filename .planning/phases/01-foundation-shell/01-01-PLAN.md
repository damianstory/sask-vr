---
phase: 01-foundation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/post-vr/page.tsx
  - styles/globals.css
  - content/types.ts
  - content/carpentry.json
  - lib/utils.ts
  - next.config.ts
  - tsconfig.json
  - package.json
  - .prettierrc.json
  - vitest.config.ts
  - tests/content-schema.test.ts
  - tests/landing.test.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - PERF-05

must_haves:
  truths:
    - "Student sees two large tap targets on the landing page labeled 'I'm about to do VR' and 'I just finished VR'"
    - "Tapping Pre-VR card navigates to /pre-vr route"
    - "Tapping Post-VR card navigates to /post-vr route"
    - "Post-VR page is directly accessible via URL /post-vr"
    - "Landing page cards stack vertically on mobile and sit side-by-side on tablet+"
    - "All screen content is defined in carpentry.json and typed by OccupationContent interface"
    - "carpentry.json contains placeholder data for all 7 screen sections"
    - "Project builds successfully for static deployment"
  artifacts:
    - path: "content/types.ts"
      provides: "OccupationContent TypeScript interface"
      exports: ["OccupationContent"]
    - path: "content/carpentry.json"
      provides: "All placeholder content for carpentry occupation"
      contains: "screenOne"
    - path: "app/page.tsx"
      provides: "Landing page with path selection cards"
      min_lines: 30
    - path: "app/post-vr/page.tsx"
      provides: "Post-VR bridge page shell"
      min_lines: 10
    - path: "app/layout.tsx"
      provides: "Root layout with Open Sans font and global styles"
      min_lines: 15
    - path: "styles/globals.css"
      provides: "Tailwind directives, brand color tokens, spacing tokens"
      contains: "--myb-primary-blue"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with React plugin and jsdom"
      min_lines: 8
    - path: "tests/content-schema.test.ts"
      provides: "Content schema validation tests"
      min_lines: 10
    - path: "tests/landing.test.tsx"
      provides: "Landing page render and navigation tests"
      min_lines: 15
  key_links:
    - from: "app/page.tsx"
      to: "/pre-vr"
      via: "useRouter().push('/pre-vr')"
      pattern: "router\\.push.*pre-vr"
    - from: "app/page.tsx"
      to: "/post-vr"
      via: "useRouter().push('/post-vr')"
      pattern: "router\\.push.*post-vr"
    - from: "content/carpentry.json"
      to: "content/types.ts"
      via: "JSON conforms to OccupationContent interface"
      pattern: "OccupationContent"
---

<objective>
Scaffold the Next.js project, set up test infrastructure (Wave 0), define the content architecture (TypeScript types + carpentry.json), configure brand design tokens, and build the landing page with two path selection cards.

Purpose: Establishes the project foundation that all subsequent work builds on -- routing, content schema, design tokens, test harness, and the entry point students see first.
Output: A running Next.js app with landing page, brand tokens, typed content file, /post-vr route shell, and vitest test infrastructure with initial tests.
</objective>

<execution_context>
@/Users/damianmatheson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/damianmatheson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Set up vitest test infrastructure (Wave 0)</name>
  <files>
    package.json, vitest.config.ts, tests/content-schema.test.ts, tests/landing.test.tsx
  </files>
  <action>
1. Install test dependencies:
```bash
npm install --save-dev vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom
```

2. Create `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
    include: ['tests/**/*.test.{ts,tsx}'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
})
```

3. Create `tests/content-schema.test.ts` — validates carpentry.json against OccupationContent:
```typescript
import { describe, it, expect } from 'vitest'
import content from '@/content/carpentry.json'
import type { OccupationContent } from '@/content/types'

describe('Content Schema', () => {
  const data = content as OccupationContent

  it('has meta section with occupationId', () => {
    expect(data.meta).toBeDefined()
    expect(data.meta.occupationId).toBe('carpentry')
    expect(data.meta.occupationTitle).toBeTruthy()
  })

  it('has all seven screen sections', () => {
    expect(data.screenOne).toBeDefined()
    expect(data.screenTwo).toBeDefined()
    expect(data.screenThree).toBeDefined()
    expect(data.screenFour).toBeDefined()
    expect(data.screenFive).toBeDefined()
    expect(data.screenSix).toBeDefined()
    expect(data.postVr).toBeDefined()
  })

  it('has no empty string values in meta', () => {
    expect(data.meta.occupationId).not.toBe('')
    expect(data.meta.occupationTitle).not.toBe('')
    expect(data.meta.displayName).not.toBe('')
    expect(data.meta.landingDescription).not.toBe('')
  })

  it('has populated arrays in each screen section', () => {
    expect(data.screenOne.stats.length).toBeGreaterThan(0)
    expect(data.screenTwo.tiles.length).toBeGreaterThan(0)
    expect(data.screenThree.employers.length).toBeGreaterThan(0)
    expect(data.screenFour.steps.length).toBeGreaterThan(0)
    expect(data.screenFive.icons.length).toBeGreaterThan(0)
    expect(data.screenSix.prompts.length).toBeGreaterThan(0)
    expect(data.postVr.checklist.length).toBeGreaterThan(0)
  })
})
```

4. Create `tests/landing.test.tsx` — validates landing page rendering and navigation targets:
```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import '@testing-library/jest-dom'

// Mock next/navigation
const mockPush = vi.fn()
vi.mock('next/navigation', () => ({
  useRouter: () => ({ push: mockPush }),
}))

// Import after mocks are set up
import Page from '@/app/page'

describe('Landing Page', () => {
  beforeEach(() => {
    mockPush.mockClear()
  })

  it('renders two path selection buttons', () => {
    render(<Page />)
    const buttons = screen.getAllByRole('button')
    expect(buttons.length).toBeGreaterThanOrEqual(2)
  })

  it('displays Pre-VR and Post-VR card text', () => {
    render(<Page />)
    expect(screen.getByText(/about to do VR/i)).toBeInTheDocument()
    expect(screen.getByText(/just finished VR/i)).toBeInTheDocument()
  })

  it('navigates to /pre-vr when Pre-VR card is clicked (LAND-01)', () => {
    render(<Page />)
    const preVrButton = screen.getByLabelText(/pre-vr/i)
    fireEvent.click(preVrButton)
    expect(mockPush).toHaveBeenCalledWith('/pre-vr')
  })

  it('navigates to /post-vr when Post-VR card is clicked (LAND-01)', () => {
    render(<Page />)
    const postVrButton = screen.getByLabelText(/post-vr/i)
    fireEvent.click(postVrButton)
    expect(mockPush).toHaveBeenCalledWith('/post-vr')
  })

  it('has aria-labels on both cards', () => {
    render(<Page />)
    expect(screen.getByLabelText(/pre-vr/i)).toBeInTheDocument()
    expect(screen.getByLabelText(/post-vr/i)).toBeInTheDocument()
  })
})
```

NOTE: These tests will fail (RED) until Tasks 2 and 3 create the content files and landing page. This is expected -- Wave 0 establishes the test harness and defines expected behaviors. Tests going GREEN is the verification signal for subsequent tasks.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -5; echo "Exit ignored -- tests expected to fail until content/pages exist"</automated>
  </verify>
  <done>Vitest installed and configured with jsdom + React plugin. Test files exist for content schema validation (CONT-01, CONT-02, CONT-04) and landing page behavior (LAND-01, LAND-02). Tests are in RED state awaiting implementation in Tasks 1-3.</done>
</task>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with brand tokens and tooling</name>
  <files>
    package.json, next.config.ts, tsconfig.json, app/layout.tsx, styles/globals.css, lib/utils.ts, .prettierrc.json
  </files>
  <read_first>
    .planning/phases/01-foundation-shell/01-RESEARCH.md
    .planning/phases/01-foundation-shell/01-UI-SPEC.md
    .planning/codebase/STACK.md
    .planning/codebase/CONVENTIONS.md
    DESIGN_SPECS.md
  </read_first>
  <action>
1. Run `npx create-next-app@latest career-explorer --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"` in the project root. After scaffolding, move all generated files from the `career-explorer/` subdirectory up to the project root and delete the empty subdirectory.

2. Install dev tooling: `npm install --save-dev prettier eslint-config-prettier`

3. Create `.prettierrc.json`:
```json
{
  "singleQuote": true,
  "trailingComma": "es5",
  "semi": false,
  "printWidth": 80
}
```

4. Create `lib/utils.ts` with a `cn()` className merger utility:
```typescript
import { type ClassValue, clsx } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
Install clsx and tailwind-merge: `npm install clsx tailwind-merge`

5. Configure `styles/globals.css` with Tailwind v4 directives and ALL brand tokens as CSS custom properties. Use `@import "tailwindcss"` (v4 syntax). Define tokens in a `@theme` block:

**Colors (exact hex values):**
- `--myb-primary-blue: #0092FF`
- `--myb-navy: #22224C`
- `--myb-light-blue: #C6E7FF`
- `--myb-off-white: #F6F6FF`
- `--myb-neutral-1: #E5E9F1`
- `--myb-neutral-2: #D9DFEA`
- `--myb-neutral-3: #AAB7CB`
- `--myb-neutral-4: #65738B`
- `--myb-neutral-5: #485163`
- `--myb-neutral-6: #252A33`
- `--myb-blue-dark: #0070CC`
- `--myb-blue-vivid: #3DA8FF`
- `--myb-navy-light: #3A3A6B`
- `--myb-light-blue-soft: #E0F0FF`

**Spacing scale:**
- `--space-1: 4px` through `--space-7: 64px` (4, 8, 16, 24, 32, 48, 64)

**Custom animations (in CSS, not @theme):**
```css
@keyframes slide-left-enter {
  from { transform: translateX(100%); opacity: 0; }
  to { transform: translateX(0); opacity: 1; }
}

@keyframes slide-right-enter {
  from { transform: translateX(-100%); opacity: 0; }
  to { transform: translateX(0); opacity: 1; }
}

@keyframes pulse-dot {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.2); }
}

.animate-slide-left {
  animation: slide-left-enter 400ms ease-out;
}

.animate-slide-right {
  animation: slide-right-enter 400ms ease-out;
}

.animate-pulse-dot {
  animation: pulse-dot 2000ms ease-in-out infinite;
}

@media (prefers-reduced-motion: reduce) {
  .animate-slide-left,
  .animate-slide-right,
  .animate-pulse-dot {
    animation: none;
  }
}
```

Also set base body styles: `bg-[var(--myb-off-white)]`, `font-sans`, `antialiased`, `text-[var(--myb-neutral-5)]`.

6. Configure `app/layout.tsx`:
- Load Open Sans via `next/font/google` with subsets: `['latin']`, display: `'swap'`, variable: `'--font-open-sans'`, weights: `[300, 900]`
- Set metadata: title "Carpentry Career Explorer", description "Explore what carpentry in Saskatchewan is really like"
- Apply font variable class to `<html>` element
- Import `@/styles/globals.css`
- Set `lang="en"` on html element

7. Ensure `tsconfig.json` has `"strict": true` and path alias `"@/*": ["./*"]`.
  </action>
  <verify>
    <automated>npm run build && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next", "react", "tailwindcss", "clsx", "tailwind-merge", "prettier"
    - styles/globals.css contains "--myb-primary-blue: #0092FF"
    - styles/globals.css contains "--myb-navy: #22224C"
    - styles/globals.css contains "--space-3: 16px"
    - styles/globals.css contains "@keyframes slide-left-enter"
    - styles/globals.css contains "@keyframes pulse-dot"
    - styles/globals.css contains "prefers-reduced-motion"
    - app/layout.tsx contains "Open_Sans" and "next/font/google"
    - app/layout.tsx contains "Carpentry Career Explorer"
    - lib/utils.ts contains "export function cn"
    - tsconfig.json contains "strict": true
    - .prettierrc.json contains "singleQuote": true
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Next.js app scaffolded with Tailwind v4, brand tokens, animations, Open Sans font, cn() utility, and Prettier config. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Define content schema and populate carpentry.json</name>
  <files>
    content/types.ts, content/carpentry.json
  </files>
  <read_first>
    TECH_SPECS.md
    .planning/phases/01-foundation-shell/01-CONTEXT.md
    .planning/phases/01-foundation-shell/01-RESEARCH.md
  </read_first>
  <action>
1. Create `content/types.ts` with the `OccupationContent` interface. The interface must define the complete schema with top-level keys matching screens:

```typescript
export interface OccupationContent {
  meta: {
    occupationId: string
    occupationTitle: string
    displayName: string
    landingDescription: string
  }
  screenOne: {
    hookQuestion: string
    salary: {
      amount: number
      label: string
      source: string
    }
    stats: Array<{
      value: string
      label: string
    }>
  }
  screenTwo: {
    heading: string
    subtext: string
    instruction: string
    minSelections: number
    maxSelections: number
    tiles: Array<{
      id: string
      title: string
      description: string
      illustrationPath: string
    }>
  }
  screenThree: {
    heading: string
    subtext: string
    employers: Array<{
      id: string
      name: string
      description: string
      employeeCount: number
      quote?: string
      logoPath?: string
      pinPosition: { x: number; y: number }
    }>
  }
  screenFour: {
    heading: string
    subtext: string
    steps: Array<{
      id: string
      title: string
      subtitle: string
      details: {
        courses?: string[]
        duration?: string
        earnings?: string
        programs?: string[]
        description: string
      }
    }>
  }
  screenFive: {
    heading: string
    subtext: string
    nameInputLabel: string
    nameInputPlaceholder: string
    iconSelectionLabel: string
    icons: Array<{
      id: string
      label: string
      svgPath: string
    }>
    downloadButtonLabel: string
  }
  screenSix: {
    heading: string
    subtext: string
    prompts: Array<{
      id: string
      text: string
    }>
  }
  postVr: {
    congratsHeading: string
    congratsSubtext: string
    checklistHeading: string
    checklist: Array<{
      id: string
      label: string
    }>
    myblueprintLink: {
      url: string
      label: string
    }
  }
}
```

2. Create `content/carpentry.json` conforming to `OccupationContent`. Use OBVIOUSLY fake placeholder data per CONTEXT.md decision:

- `meta.occupationId`: `"carpentry"`
- `meta.occupationTitle`: `"Carpentry Career Explorer"`
- `meta.displayName`: `"Carpentry"`
- `screenOne.salary.amount`: `99999` (obviously fake)
- `screenOne.hookQuestion`: `"How much does a carpenter in Saskatchewan actually make?"`
- `screenOne.stats`: 3 items with values like `"9,999"`, `"99%"`, `"999+"` and labels `"Jobs in Saskatchewan"`, `"Demand Growth"`, `"Employers Hiring"`
- `screenTwo.tiles`: 6 tiles with ids `"task-1"` through `"task-6"`, titles `"Task 1"` through `"Task 6"`, descriptions `"Description for task 1"` etc., illustration paths `"/illustrations/task-1.svg"` etc.
- `screenTwo.minSelections`: `2`, `screenTwo.maxSelections`: `3`
- `screenThree.employers`: 5 employers with ids `"employer-a"` through `"employer-e"`, names `"Company A"` through `"Company E"`, employee counts like `99`, pin positions spread across a grid (e.g., `{x: 20, y: 30}`, `{x: 50, y: 45}`, etc.)
- `screenFour.steps`: 5 steps starting with `"You are here - Grade 7/8"`, then `"High School"`, `"Post-Secondary"`, `"Apprenticeship"`, `"Journeyperson"`
- `screenFive.icons`: 6 icons with ids `"icon-hammer"`, `"icon-saw"`, `"icon-hardhat"`, `"icon-tape"`, `"icon-goggles"`, `"icon-level"` and svg paths like `"/icons/hammer.svg"`
- `screenSix.prompts`: 3 prompts with placeholder text like `"What tools do you notice being used?"`
- `postVr.checklist`: 6 items with ids `"check-1"` through `"check-6"`, labels like `"Checklist item 1"`
- `postVr.myblueprintLink.url`: `"https://www.myblueprint.ca"`, label: `"Open myBlueprint"`
- ALL text fields (headings, subtext, instructions, button labels) must be populated in the JSON -- no empty strings

3. Verify the JSON conforms to the TypeScript interface by importing it in a temporary check (or rely on the type assertion pattern from RESEARCH.md).
  </action>
  <verify>
    <automated>npm run type-check && npx vitest run tests/content-schema.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - content/types.ts contains "export interface OccupationContent"
    - content/types.ts contains "screenOne" and "screenTwo" and "screenThree" and "screenFour" and "screenFive" and "screenSix" and "postVr"
    - content/types.ts contains "meta:" with "occupationId"
    - content/carpentry.json contains "carpentry" (occupationId)
    - content/carpentry.json contains "99999" (placeholder salary)
    - content/carpentry.json contains "task-1" (tile id)
    - content/carpentry.json contains "Company A" (employer name)
    - content/carpentry.json contains "Grade 7/8" (pathway step)
    - content/carpentry.json contains "icon-hammer" (icon id)
    - content/carpentry.json contains "check-1" (checklist id)
    - content/carpentry.json contains "myblueprint.ca" (link url)
    - content/carpentry.json has no empty string values ("": should not appear as a value)
    - `npm run type-check` exits 0
    - `npx vitest run tests/content-schema.test.ts` passes (all GREEN)
  </acceptance_criteria>
  <done>OccupationContent interface defines the complete content schema. carpentry.json has obviously-fake placeholder data for all 7 screen sections plus meta. TypeScript type-check passes. Content schema tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Build landing page with path cards and Post-VR route shell</name>
  <files>
    app/page.tsx, app/post-vr/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-shell/01-UI-SPEC.md
    .planning/phases/01-foundation-shell/01-CONTEXT.md
    DESIGN_SPECS.md
    app/layout.tsx
    styles/globals.css
  </read_first>
  <action>
1. Create `app/page.tsx` as a client component ('use client') with the landing page:

**Structure:**
- `<main>` with full viewport height, centered content, Off-White background
- myBlueprint logo placeholder at top (use a simple text placeholder or `<img>` pointing to `/logos/myblueprint-logo.svg` -- the file won't exist yet, which is fine)
- `<h1>` with text "Carpentry Career Explorer" -- styled 40px/900/Navy on desktop, 28px on mobile
- Two `<button>` elements (NOT divs, NOT links) for path cards

**Pre-VR Card:**
- Heading: `"I'm about to do VR"` -- 24px (desktop) / 20px (mobile), weight 900, Navy color
- Subtext: `"Learn what carpentry in Saskatchewan is really like."` -- 16px, weight 300, Neutral-5
- Background: Light Blue `#C6E7FF`
- Sizing: `min-h-[180px] md:min-h-[280px]`, full-width on mobile, `flex-1` on tablet+
- `onClick`: `router.push('/pre-vr')` using `useRouter` from `next/navigation`
- `aria-label`: `"Start Pre-VR experience: Learn what carpentry in Saskatchewan is really like"`
- Hover: shadow lift `shadow-[0_8px_24px_rgba(34,34,76,0.1)]`, border shifts to Primary Blue
- Focus: `focus:outline-none focus:ring-[3px] focus:ring-[var(--myb-primary-blue)] focus:ring-offset-[3px]`
- Border radius: `rounded-xl` (12px)
- Padding: `p-6`

**Post-VR Card:**
- Heading: `"I just finished VR"` -- same styling as Pre-VR card heading
- Subtext: `"Reflect on what you just experienced."` (per UI-SPEC copywriting contract)
- Same visual styling, sizing, hover, focus as Pre-VR card
- `onClick`: `router.push('/post-vr')`
- `aria-label`: `"Go to Post-VR reflection: Reflect on your VR carpentry experience"`

**Layout:**
- Cards container: `flex flex-col gap-4 md:flex-row md:gap-6` with `max-w-[824px]` (400px per card + 24px gap)
- Cards stack vertically on mobile, side-by-side on tablet+
- All touch targets well above 44x44px minimum (cards are 180px+ tall)

2. Create `app/post-vr/page.tsx` as a minimal shell:
- Simple page with `<main>` containing a heading "Post-VR Reflection" and a paragraph "This page will contain the post-VR checklist."
- Styled with Navy heading, Neutral-5 body text
- This page must be independently accessible at /post-vr (no session state dependency)
- No 'use client' needed for this shell -- it's a static server component

3. Create the `public/logos/` directory (empty for now -- logo file will be added later).
  </action>
  <verify>
    <automated>npm run build && npm run type-check && npx vitest run tests/landing.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains "'use client'"
    - app/page.tsx contains "I'm about to do VR" or "I&apos;m about to do VR"
    - app/page.tsx contains "I just finished VR"
    - app/page.tsx contains "Learn what carpentry in Saskatchewan is really like"
    - app/page.tsx contains "Reflect on what you just experienced"
    - app/page.tsx contains "router.push('/pre-vr')"
    - app/page.tsx contains "router.push('/post-vr')"
    - app/page.tsx contains "<button" (semantic button elements, not divs)
    - app/page.tsx contains "aria-label"
    - app/page.tsx contains "min-h-[180px]"
    - app/page.tsx contains "md:flex-row" or similar responsive layout class
    - app/post-vr/page.tsx exists and contains "Post-VR"
    - `npm run build` exits 0
    - `npx vitest run tests/landing.test.tsx` passes -- confirms LAND-01 navigation behavior
  </acceptance_criteria>
  <done>Landing page renders two large path cards using semantic buttons with correct copy, hover/focus states, and responsive stacking. Post-VR route exists at /post-vr as an independent shell. All touch targets exceed 44x44px. Build succeeds. Landing tests confirm navigation to /pre-vr and /post-vr routes (LAND-01).</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (confirms static generation works for Vercel deployment)
- `npm run type-check` passes (TypeScript strict mode with no errors)
- `npx vitest run` passes (content schema + landing page tests GREEN)
- Landing page renders at localhost:3000 with two path cards
- /post-vr is directly accessible via URL
- carpentry.json has data for all 7 screen sections
- Brand tokens are defined in globals.css
- Slide and pulse animations are defined in CSS with reduced-motion fallback
</verification>

<success_criteria>
- Project builds and serves locally
- Landing page shows "Carpentry Career Explorer" heading with two path cards
- Pre-VR card navigates to /pre-vr, Post-VR card navigates to /post-vr
- Cards are semantic `<button>` elements with aria-labels
- Cards stack on mobile, side-by-side on tablet+
- OccupationContent interface defines complete schema
- carpentry.json has obviously-fake placeholder data for every screen section
- Brand color tokens, spacing tokens, and animation keyframes exist in globals.css
- prefers-reduced-motion disables all animations
- Content schema tests pass (CONT-01, CONT-02, CONT-04)
- Landing page tests pass (LAND-01, LAND-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shell/01-01-SUMMARY.md`
</output>
